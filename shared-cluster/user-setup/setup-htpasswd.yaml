# This file is used as a reference for manual OAuth configuration
# The bootstrap.sh script handles the actual patching automatically

# Example OAuth patch that gets applied:
# oc patch oauth cluster --type='merge' -p '{
#   "spec": {
#     "identityProviders": [
#       {
#         "name": "test-user",
#         "mappingMethod": "claim",
#         "type": "HTPasswd",
#         "htpasswd": {
#           "fileData": {
#             "name": "htpass-secret-test-user"
#           }
#         }
#       }
#     ]
#   }
# }'
